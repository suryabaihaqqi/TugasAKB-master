<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.TUGAS_UAS_AKB_AKB1_10116008">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logolauncher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presenter.MainActivityBottom"
            android:label="@string/title_activity_main_bottom"></activity>
        <activity android:name=".presenter.MainActivityLogin" />
        <activity android:name=".presenter.viewpager" />
        <activity android:name=".presenter.MainActivity" 
        <activity android:name=".presenter.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>